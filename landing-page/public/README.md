# public/

Drop site assets here. Notably:

- `videopilot-demo.mp4` — the demo video the hero showcase points at. A
  sibling agent populates this; the page renders gracefully when it's
  missing.
- `videopilot-demo-poster.png` — optional poster frame for the `<video>`
  element. Not currently referenced; add it and wire it up via the
  `poster` attribute in `components/video-showcase.tsx` if desired.
